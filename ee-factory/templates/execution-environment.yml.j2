version: 3

images:
  base_image:
    name: "{{ builder_image | default('192.168.15.20:5000/ee-builder:1.1') }}"

dependencies:
  galaxy: requirements.yml

options:
  package_manager_path: /usr/bin/microdnf
