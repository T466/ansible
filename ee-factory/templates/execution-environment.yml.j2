version: 3

images:
  base_image:
    name: "{{ target_base_image }}"

dependencies:
  galaxy: requirements.yml
  python:
    - ansible-pylibssh>=0.2.0

options:
  package_manager_path: /usr/bin/microdnf
